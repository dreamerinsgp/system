Q1:创建账户的核心逻辑？

1）验证目标账户为空
行为：检查账户余额是否为0
目的：防止覆盖已有账户

2）分配空间并设置所有者 
行为： 分配数据空间；设置程序所有者；验证签名
目的： 初始化账户结构和所有权

3）转账Lamports 
行为：从源账户转账到新账户，验证签名和余额
目的：为新账户提供初始资金


Q2: 分配空间并设置所有者相关问题
代码： allocate_and_assign(&mut to, to_address, space, owner, signers, invoke_context)?;

Q2.1: 分配空间做了哪些事情？
1) 验证签名: 确保账户已签名，防止未授权操作
  if !address.is_signer(signers) {
        ic_msg!(
            invoke_context,
            "Allocate: 'to' account {:?} must sign",
            address
        );
        return Err(InstructionError::MissingRequiredSignature);
    }
Q: 验证什么账户？ 如何验证的？
账户：验证的是目标账户(to_account), 既要创建的新账户
如何验证： 
获取交易签名者集合 (signers: HashSet<Pubkey>)
调用address.is_signer(signers)方法
    seed-based账户：检查base pubkey是否在signers中
    普通账户： 检查address pubkey 是否在signers中





2) 检查账户是否已被使用
Q: 如何检查？


3）验证空间大小？
Q: 大小限制？


4）设置数据长度
目的：时机分配指定大小的数据空间






2）任命owner做了哪些事？